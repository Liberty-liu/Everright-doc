export default {
  paths () {
    const result = [
      { params: { type: 'linear' } },
      { params: { type: 'matrix' } },
      { params: { type: 'quick-search' } },
      { params: { type: 'quick-filter' } }
    ]
    return result
  }
}
